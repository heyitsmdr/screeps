name: Push to Screeps

on:
  push:
    branches:
      - main

jobs:
  push:
    name: Push
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
        with:
          node-version: 16
          cache: 'npm'